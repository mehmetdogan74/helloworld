jobs:
- deployment:
  environment: dev
  pool:
    name: Azure Pipelines
  strategy:                 
    runOnce:            
      deploy: 
        steps:       
        - script: docker ps
- deployment: deployDB
  environment: dev
  pool:
    name: Azure Pipelines
  strategy:                 
    runOnce:
      deploy: 
        steps:       
        - script: docker ps