pool:
   name: Live Cluster
stages:
  - stage: DeployLive
    displayName: Deploylive
    jobs:
    - deployment: deploymentjob
      displayName: Deployment Job
      environment:
       name: Live
       resourceType: VirtualMachine
      strategy:                 
        runOnce:
          deploy: 
            steps:       
            - script: |
               hostname
               date
               whoami
               cd docker-geoserver
               git status
              displayName: Testing live cluster